# CO-101 — Master Change-Order Ledger (Consolidated Register, CO-1–CO-34)

| Field | Value |
|---|---|
| **Change Order** | CO-101 |
| **Title** | Master Change-Order Ledger — Consolidated Register |
| **Status** | ACTIVE — single source of truth for every Change Order, effective 12 Jul 2026 |
| **Authority** | Chief Architect, TimeTribe |
| **Date** | 12 Jul 2026 |
| **Governs** | Platform-wide. Not a cell — this is the change-order register itself. |
| **Repo mount** | `/docs/change-orders/CO-101.md` |
| **Supersedes (process only)** | The one-`CO-<N>.md`-file-per-order convention, for CO-30 onward (originally stated as "CO-28 onward," then "CO-29 onward" after the CO-27/28 collision amendment — moved again to CO-30 once CO-29 itself was filed directly into this ledger, correctly, under the same rule). No historical CO content is altered — CO-1–CO-29 rulings stand exactly as ratified; this ledger indexes and consolidates them into one file and is the sole location for everything after it. |

---

## 0. Operating Rule (read first)

Effective this filing, TimeTribe stops minting one `CO-<N>-<slug>.md` file per change order. **Every past and future change order lives in this one file.**

**Why.** Auditing the 27-file corpus to build this ledger surfaced three failure modes that a single running file structurally prevents:
1. **A numbering collision** — two independently-ratified documents both titled themselves CO-26 (§4.1). **A second, same-shape collision (§4.8) surfaced hours later, the day of this same filing** — this ledger's own consolidation audit missed a live CO-27 document when resolving the first collision, inadvertently reassigning an already-claimed number. Proof the single-ledger convention below is necessary, not just tidy: even the audit that motivated it wasn't immune until every past CO lived in one place.
2. **Orphaned citations** — CO-18, CO-22, CO-23, CO-24 are referenced by number in other ratified documents but have no back-filled standalone source file in project knowledge (§4.2–4.4).
3. **A stale repo-mount snapshot** — the `/mnt/project` copy of CO-20 predates its own §0.4 audit-resolution amendment; the live, current version disagrees with the mounted one (§4.5).

**Going forward:**
- **New change order** → append a new dated entry under §2, next available number. No new file.
- **Amendment to a past CO** → a new `§0`-style amendment note *inside that CO's entry in this ledger*, same in-place-correction pattern CO-20/CO-21 already established. No ruling is silently deleted — only corrected or superseded with a note, same as always.
- **After every edit to this file**, log the change via the memory tool so the convention and the latest state survive across sessions without re-reading this file end to end.
- The historical `CO-16` through `CO-29` files remain on disk, unmodified, as the pre-consolidation audit trail. They are not deleted and are no longer the authoritative record going forward — this ledger is.

---

## 1. Master Index

| # | Title | Date | Status | Governs | Sign-off |
|---|---|---|---|---|---|
| CO-1 | B2B Lexicon → v1.1 | 06 Jul 2026 | RATIFIED | B2B Lexicon | CA (B-05: Brand+CA joint) |
| CO-2 | B2C Lexicon → v1.1 | 06 Jul 2026 | RATIFIED | B2C Lexicon | CA (B-11: Brand+CA joint) |
| CO-3 | Marketing Web Lexicon → v1.1 | 06 Jul 2026 | RATIFIED | Marketing Web Lexicon | Brand+Growth+CA joint |
| CO-4 | Brand DNA → v3.1 | 06 Jul 2026 | RATIFIED | Brand DNA | Brand+CA joint |
| CO-5 | HQ Lexicon v1.0 (new) | 06 Jul 2026 | RATIFIED (ruling); doc-completion unconfirmed — §4.6 | HQ Web lexicon | CA authors; Brand reviews |
| CO-6 | Gold Standard → v2.19 | 06 Jul 2026 | RATIFIED | Network topology, AU cell | CA + Platform Architect |
| CO-7 | Backend Pipeline → v1.1 | 06 Jul 2026 | RATIFIED | Constitution C.1.5/C.2, pool matrix | CA |
| CO-8 | Frontend Pipeline → v1.2 | 06 Jul 2026 | RATIFIED | Stage 2C domain, banned-token Stage 0 | CA |
| CO-9 | part8 → v1.2 + Concierge Retool reclass | 06 Jul 2026 | RATIFIED | Concierge Desk → HQ Web migration | CA + Compliance |
| CO-10 | Spec-8 HQ Web → v1.2 | 06 Jul 2026 | RATIFIED | HQ Web wire fix, `hq.audit` convention | CA |
| CO-11 | SPEC-8 Assignment → v1.1 | 06 Jul 2026 | RATIFIED | EntitlementsTable re-scope, tier rename | CA + Pricing Auditor |
| CO-12 | Admission_Gate_values → v1.1 | 06 Jul 2026 | RATIFIED; superseded → v2 by CO-17 | Admission floors, single ownership | Pricing Auditor |
| CO-13 | Monetization Audit → v2.0 | 06 Jul 2026 | RATIFIED | Noun refresh vs pricing v1.4 | Brand + Pricing Auditor |
| CO-14 | Master-spec sweep (part1, part7) | 06 Jul 2026 | RATIFIED | Cache-invalidation fix, MVP-token sweep | CA |
| CO-15 | Project Instructions → v3.1 | 06 Jul 2026 | RATIFIED (ruling); NOT reflected in live v3 — §4.7 | Phasing paragraph, pool wording | CA (owner sign-off) |
| CO-16 | Vertical-Aware Seat Titling | not stated in source | RATIFIED | `studios.verticals[]`, Seat display-title | CA (+ Brand joint, display rebind) |
| CO-17 | Admission ⇄ Tier Separation | not stated in source | RATIFIED (structure); floor **values** = Growth/Founders | Admission Gate, entitlements, 7 pricing cells | CA; floors pending Growth/Founders |
| CO-18 | *(Tab: underline, label-only, sole section-nav)* | unknown | CITATION-ONLY — §4.2 | B2B Web Tab component (v0) | unknown |
| CO-19 | Services Shell: Inspector Width & List Conformance | 11 Jul 2026 (back-filled) | RATIFIED — back-filled | B2B Web Services shell | CA |
| CO-20 | Native Window-Class & Adaptive Shell | 11 Jul 2026 | RATIFIED — amended, audit resolved | Seat Command + Black Mirror shells | CA |
| CO-21 | Chairside POS Peripheral Hardware Posture | 12 Jul 2026 | RATIFIED — amended | Checkout/POS scanner, drawer, terminal BYO | CA |
| CO-22 | Single-Pane Content Max-Width — Native Medium/Expanded | 12 Jul 2026 | RATIFIED — was citation-only, full text restored §4.10 | `mobile-shell` Settings + any standalone single-column screen | CA |
| CO-23 | Schedule Window-Class Conformance (Inspector) & In-Screen Persistent Action Toolbar | 12 Jul 2026 | RATIFIED — was citation-only, full text restored §4.10 | `mobile-calendar` (M-02) | CA |
| CO-24 | Quantum Schedule Ground Truth | 12 Jul 2026 | RATIFIED — was citation-only, full text restored §4.10 | `mobile-calendar` (M-02) reconciliation | CA |
| CO-25 | Expanded-Tier Feature Parity (amends CO-24 R10) | 12 Jul 2026 | RATIFIED | mobile-calendar (M-02) window-class gating | CA |
| CO-26 | Web Touch & Kiosk Compliance | 12 Jul 2026 | RATIFIED | B2B Web touch/kiosk doctrine | CA |
| CO-27 | Role-Gated Navigation Matrix & Member/Role ID Surfacing *(renumbered — was duplicate CO-26)* | 12 Jul 2026 | RATIFIED — renumbered, §4.1 | `mobile-shell` nav-config | CA |
| CO-28 | Window-Class-Conditional Tab Label — Today/Schedule *(renumbered — was duplicate CO-27)* | 12 Jul 2026 | RATIFIED — renumbered, §4.8 | `mobile-shell` nav-config label | CA |
| CO-29 | Single-Pane Content Width, Pre-Auth Screens (Sign-in, MFA, Bootstrap) | 12 Jul 2026 | RATIFIED — new this filing | `mobile-shell` pre-auth surfaces | CA |
| CO-30 | Service Delivery Mode & Route Tab Capability Gating | 13 Jul 2026 | RATIFIED — new this filing | `mobile-shell` (Route gating) · `studio-settings` · `roster-compensation` | CA |
| CO-31 | Stylist View-Scope: Own + Assisted Clamp | 13 Jul 2026 | RATIFIED (ruling); code fix PENDING — renumbered, see §2.3, §4.11 | Quantum Schedule backend, B2B Web scheduler, `mobile-calendar` (M-02) | CA |
| CO-32 | Add-on Catalog Cascade — MULTI_LOC, Bundle Rename, Utilisation Guard SKU | 13 Jul 2026 | RATIFIED — new this filing; R3 tier-eligibility OPEN | Pricing Data v2.0 §4.3/§4.5/§5.1/§5.2, entitlements SKU registry | CA; R3 tier pending Growth/Founders |
| CO-33 | Add-on Feature-Gate Definitions (Console Telemetry Pro, Revenue Artillery Pro, Lookbook Accelerator) & `AI_CONCIERGE`→`AI_COMMAND` Rename | 13 Jul 2026 | RATIFIED — new this filing; gates not yet built | pkg B2B-W-06, pkg B2B-W-05, B2C Lookbook spec, entitlements SKU registry, Cognito B2B token contract | CA |
| CO-34 | Standard-vs-Add-on Market-Fit Correction — closes CO-32 R3, amends CO-33 R1/R2/R4, new Loyalty Ladder tier-inclusion | 13 Jul 2026 | RATIFIED — new this filing; R6 (Surge Shield) still open | Pricing Data v2.0, pkg B2B-W-06, Yield Shield spec, entitlements SKU registry, all 7 `pricing-plan-*.md` | CA |

---

## 2. Full Register

### 2.1 — Lexicon & Brand Governance (CO-1 – CO-5)

**Source:** *Tri-Lexicon Validation Report v1.0* — B2B/B2C/Marketing Web Lexicons adjudicated against Brand DNA v3.0, Project Instructions v3, Pricing Data v2.0. Panel: Brand Custodian, Gulf Consumer UX Writer, Arabic Localization & RTL Lead, KSA/UAE Market Analyst, Compliance Counsel, Chief Architect, Surface-Leak Auditor. **Adjudicated 06 Jul 2026.**

| CO | Document → version | Key rulings folded in | Sign-off |
|---|---|---|---|
| **CO-1** | B2B Lexicon → **v1.1** | **B-01** Watchtower HUD deprecated → canonical **Ops HUD**. **B-02** Console (HQ) re-bound to HQ Web (role-scoped); Retool renamed **Ops Panel (Retool)**, impersonation issues from HQ Web only. **B-05** "Sniper Scope" stays internal codename; UI noun = **Audience Scope** / «نطاق الجمهور» (Tier-2 display rebind, Brand+CA joint). **B-08** new Constitution clause 6: all B2B user-facing payloads ship `BilingualText {en, ar}`. Plus §3.2 fixes, Surface-Scope column, AR Registry appendix. | B-05: Brand+CA joint. Rest: CA |
| **CO-2** | B2C Lexicon → **v1.1** | **B-03** Checkout CTA for `redeem` = **"Redeem"** / «استبدال» ("Apply" stays B2B-only). **B-04** "Explode" reclassed SCHEMA/INTERNAL-only; client CTA = **"Book separately"** / «حجز منفصل». **B-10** "Consent Record" disambiguated: **Lead Consent Record** (Marketing, pre-identity) vs **Client Consent Record** (B2C, identity-bound). **B-11** Loyalty "Asset" ships value-paired ("1,420 Assets · AED 142"); full display-noun rebind tabled for Brand+CA joint sign-off. Plus book/booking verb-noun rule, Entourage scope tags, §5 reserved nouns (Ramadan Hours, Studio Audience, Friday-prayer AE extension, Eid Gifting). | B-11: Brand+CA joint. Rest: CA |
| **CO-3** | Marketing Web Lexicon → **v1.1** | **B-06** Tier names (Solo/Tribe/Ecosystem) are brand nouns, **never translated** — AR renders Latin script; قبيلة added to AR forbidden-literal register. **B-07** confirms Marketing's admission-gate wording already correct (trailing-90d revenue, region currency, Pricing_Data-owned). **B-09** canonical error code = **`IDEMPOTENT_REPLAY`** (not `IDEMPOTENCY_REPLAY`). **B-10** Lead Consent Record side. Plus salon-SEO carve-out, Studio-Membership qualifier rule. | CTA/tier rules: Brand+Growth+CA joint |
| **CO-4** | Brand DNA → **v3.1** | **B-07** gate restated: trailing-90-day verified monthly revenue, region currency, values owned exclusively by Pricing_Data per region (not the £120K+ annual figure). **B-12** casing lock: **"TimeTribe"** (not "Timetribe") everywhere including Brand DNA itself. £-figures reclassified UK-illustrative only. | Brand+CA joint |
| **CO-5** | HQ Lexicon **v1.0 (new)** | Full new document: role registry (HQ-Concierge/AGM/Admin/Compliance/Read-Only) · shell-slot nouns (Concierge Desk, AGM Console, Admission Gate Review, OmniCreator Authoring, Market Control, Compliance, Analytics Deep-Dive) · `hq.audit.<action>` event family · impersonation vocabulary (post-B-02) · Ops Panel (Retool) boundary · AI-Concierge / HQ-Concierge disambiguation · federated-read vs region-selector vocabulary. Path of record: `/specs/hq-web/_master/HQ_LEXICON_v1.md`. **Unblocks every `hq web prompts` cell.** | CA authors; Brand reviews role display names |

**Other lexicon findings, resolved without a dedicated CO:** B-08 (bilingual contract) folds into CO-1. Full drift register (D-1…D-8) closes against B-01–B-12 — see original report if the drift-disposition narrative is ever needed; outcomes are fully captured in the rows above.

---

### 2.2 — Spec-Layer & Architecture Governance (CO-6 – CO-15)

**Source:** *Full-Corpus Spec Validation Report v1.0* — 69 documents cross-adjudicated: architecture spine, data/residency, auth, API discipline, pricing math, phasing conformance, pipeline contracts, brand/legal exposure. Panel adds Platform & Infra Architect, Data & Residency Officer, Identity & Auth Architect, Pricing & Monetization Auditor, DevEx & Pipeline Lead to the CO-1–5 panel. **Adjudicated 06 Jul 2026.**

**Canon Precedence Ladder** (decisive — governs every conflict; a lower document contradicting a higher one is drift, not doctrine):
1. Region Phasing Doctrine v1.1
2. Lexicon Validation v1.0 + Spec Validation Report (this section's source)
3. SpecKit Backend Pipeline v1.0 §C Global Constitution
4. Regional Pricing Plans v1.4/v1.6 (×7)
5. Spec-8 HQ Web · Spec-8 Addendum · part8 (as amended by CO-9/CO-10)
6. SpecKit Frontend Pipeline v1.1 · Agent Assignment Masters
7. Gold Standard v2.18.1/v2.19 — network topology + regional service matrix **only**
8. Master Spec parts 1–7 · design docs 01–15 · feature docs 31–47 — historical substrate, live only where not superseded

| CO | Document → version | Key rulings folded in | Sign-off |
|---|---|---|---|
| **CO-6** | Gold Standard → **v2.19** | S-01 DynamoDB swimlane rewrite (per-cell, region-pinned, TTL'd derived data — never SoR, never Global Tables). S-03 support-console row deleted. S-07 client wire routes to AppSync, not API Gateway. S-08 EventBridge is the cross-domain spine (Step Functions = intra-domain sagas only). S-11 adds the missing **AU cell row** (`au.timetribe.io`, `ap-southeast-2`, Cognito AU pool, Adyen AUD). W-1 SnapStart claim scoped to Java 21 only (not Node 20). W-4 `.app` deep-link scheme is canonical. | CA + Platform Architect |
| **CO-7** | Backend Pipeline → **v1.1** | S-11 C.1.5 generalized: *"PII and payment payloads terminate in the tenant's home cell region; never cross-region."* S-13 Cognito pool matrix codified in C.2: `b2b-<region>` / `b2c-<region>`, region-pinned, zero federation; HQ pool single, dedicated, `me-central-1`-pinned, role-scoped. S-05 rate-limit classes: **Solo / Tribe / Ecosystem / Service-to-Service** (kills phantom `Free`/`Roster`/`Enterprise`). S-09 cache-invalidation contract added. W-6 lead-capture REST ruled a codified webhook-class exception. | CA |
| **CO-8** | Frontend Pipeline → **v1.2** | S-03/W-7 Stage 2C domain fixed to `admin.timetribe.io`; pre-B-02 Retool assumptions removed. S-11 phasing header generalized. S-10 banned-token check added to Stage 0. | CA |
| **CO-9** | part8 → **v1.2** + Concierge Retool doc reclassification | S-02 resolves the Concierge tri-claim: Concierge UI migrates to **HQ Web's Concierge Desk** slot (AppSync); payload/event/AI-agent contracts port verbatim into `specs/concierge/concierge-desk/`; Retool retains only queue-drain lightweight ops (**Ops Panel**). Impersonation tokens issue from HQ Web only (HMAC STS). S-05 tier-key rename cascade. S-10 sweep. | CA + Compliance |
| **CO-10** | Spec-8 HQ Web → **v1.2** | S-14: HQ Web is an **AppSync client surface** — deletes the §0.5 REST-consumption clause; `/internal/*` REST reserved to Ops Panel (Retool) + n8n M2M. Adds cross-cutting convention: **every HQ write emits `hq.audit.<action>`** (actor, reason, target, region) alongside `audit_log`. Concierge Desk module slot added (receives S-02 contracts). | CA |
| **CO-11** | SPEC-8 Assignment → **v1.1** | S-01: `EntitlementsTable` re-scoped — Aurora `entitlements` is System of Record; per-cell DynamoDB (DAX-fronted) is an authorized read-side projection only, rebuilt from `entitlement.updated`, zero cross-region replication. S-05 rename cascade (`Roster+`/`Enterprise` → `Tribe+`/`Ecosystem`). | CA + Pricing Auditor |
| **CO-12** | Admission_Gate_values → **v1.1** | S-04: resolves 3-region floor drift (UK/EU/CA). **Regional pricing plans v1.4 become the exclusive owner of every floor value**; `Admission_Gate_values` demoted to a derived summary, regenerated from the seven v1.4 files. *(Superseded structurally by CO-17 → v2, which layers the two-decision admission/tier model on top — floor-value ownership rule from this CO still holds.)* | Pricing Auditor |
| **CO-13** | Monetization Strategy Audit → **v2.0** | S-06: refreshes "Roster Tier" / "Watchtower Forecasting" dead-noun usage against live pricing v1.4 vocabulary; values were already correct, only the noun layer was stale. | Brand + Pricing Auditor |
| **CO-14** | Master-spec sweep (part1, part7) | S-09 canonical invalidation path: `mutation Lambda → EventBridge domain event → invalidation Lambda → AppSync publish + Redis DEL`. S-10 sweeps all 4 occurrences of the banned token **"MVP"** → "Launch blockers" / "post-launch bake-off". | CA |
| **CO-15** | Project Instructions → **v3.1** | S-11 phasing paragraph rewritten to doctrine language: *"Supported regions in build (7 cells); launch per-region, board-mandated, UAE first"* — replacing hardcoded "Phase 1 = UAE… Phase 2 = KSA." S-13 pool wording aligned to CO-7's matrix. W-3 adds the `contracts/` line (openapi.yaml, schema.graphql) to the SpecKit contract. **See §4.7 — this resolution does not appear to have propagated to the live Project Instructions v3 governing this conversation.** | CA (owner sign-off) |

**Watch Register (non-blocking, resolved same session):** W-1–W-7, folded into CO-6/CO-7/CO-8 rows above. **Conformance Confirmations (validated PASS, no CO needed):** Backend Constitution C.1–C.4, Pricing v1.4 ×7 D1–D10 cascade, Pricing Surfacing Split v1.1, BillingAdapter master, Mobile Release Pipeline cell, Marketing stack, Spec-8 Addendum v1.0.

**Sequencing at time of ratification:** CO-5 first (unblocks HQ); CO-7+CO-6 next (every new cell authors against these); CO-9+CO-10 (unblocks `hq web prompts`); CO-12+CO-11 (unblocks Admission Gate/entitlement cells); CO-8/13/14/15 parallel, non-blocking.

---

### 2.3 — Product, UX & Platform Change Orders (CO-16 – CO-31)

Individually-authored, single-topic change orders. Historical files retain the `Repo mount: /docs/change-orders/CO-<N>-<slug>.md` convention shown in each entry.

---

**CO-16 — Vertical-Aware Seat Titling**
*Status: RATIFIED · Historical file: `co-16-vertical-seat-titling.md` · Sign-off: Chief Architect (+ Brand Custodian joint on the display-rebind scope)*

Origin: role × vertical coverage audit found B2C's Seat-render rule capped practitioner titles at "Stylist/Artist/Therapist" — no fit for Barber, only a stretch-fit for Nails, no fit for Brows & Lashes / Waxing.

- New **`studios.verticals[]`** array (B2B Lexicon §2.1; not single-select) — captured at Apply, persisted post-admission. Enum: `FULL_SERVICE · BARBER · NAILS · SPA · BEAUTY_SALON · BROWS_LASHES · WAXING · OTHER`. Marketplace matches on **any** array entry, not equality.
- B2C **Seat** row: client UI never renders raw "Seat" — renders via the governed `seatTitleOptions` picklist (new companion doc `seat-title-picklist-v1.md`), `seats.display_title` (new column).
- Technical Foundation v2 Doc 15 dynamic-pricing: `DIRECTOR` role condition retired (it referenced a value that never existed in the `RoleId` enum — a dead condition). Replaced: `SENIOR_STYLIST` @ 1.25×, disclosure string keyed to `{{resolved_seat_title}}`. **"Director" reserved exclusively for the B2B Owner/Member command-stance persona** — no further collision.
- RBAC `RoleId` enum untouched — display + schema only, no new role, no CA role-addition sign-off triggered.
- Cascade: Aurora `studios.verticals[]` · Sanity `seatTitleOptions` (new) · AppSync `Seat.display_title` · Studio Settings (B2B-W-12) · B2C render layer. **Code deferred to a lint-driven retrofit pass — not actioned by this CO itself.**

---

**CO-17 — Admission ⇄ Tier Separation**
*Status: RATIFIED (structure); floor **values** remain Growth/Founders sign-off · Historical file: `CO-17-admission-tier-separation.md` · Registers: D14 (reframed), D15, D16*

One word — "qualification" — was carrying two unrelated jobs: is this Studio admitted at all, and which tier does it land on. Splitting them fixed three defects: the 2–4 Seat gap, greenfield-studio lockout, and a retention-review misfire.

- **Decision 1 — Admission** (single platform bar, qualify via any one): `REVENUE` (trailing-90d ≥ Solo floor) · `APPOINTMENTS` (≥150/mo) · `REFERRAL` (member in good standing) · `SALES_VERIFIED_FOOTPRINT` (greenfield — signed lease + trade licence + installed Seats, verified via HQ Document Verification Queue).
- **Decision 2 — Tier fit** (post-admission only, highest tier whose qualifier is met, pure OR): Tribe (≥2 staffed Seats **or** Tribe revenue **or** Tribe appointments), Ecosystem (≥2 Locations **or** Eco revenue **or** Eco appointments), Solo (default).
- **D14 reframed:** the verified 2-Seat path is a Decision-2 tier-fit path only — never an admission shortcut.
- **D15:** admission = Solo-bar-or-referral-or-greenfield; tier = structural-OR-revenue-OR-appointments, symmetric (Tribe→Seats, Ecosystem→Locations). Solo floor tagged `is_admission_bar: true` only.
- **D16:** platform-retention review (HQ-AGM) keys off the **Solo bar** only, 2 consecutive cycles, revenue AND appointments — never a higher tier's floor. Structural-qualifier loss triggers a separate, softer **tier-fit review** instead.
- Marketing `/pricing`: **unchanged** — existence-only doctrine holds, zero mechanics exposed.
- Cascade: `Admission_Gate_values` → v2, all 7 `pricing-plan-*` §1/§1.1 re-authored (execution artifact: `pricing-cell-amendment-v1.md`), entitlements service, AppSync resolvers (`admissionQueue`, `decideAdmission`, `openTierFitReview`, `admissionStatus`).

---

**CO-18 — *(citation-only — see §4.2)***
Known scope from citation: **Tab component — underline indicator, label-only, sole section-navigation mechanism** (v0/web). Matches `07_timetribe_atomic_components_v3.md` Component 15 · Tab exactly (2px bottom border indicator, `role="tablist"`, roving tabindex) but no document formally cites itself as "CO-18." No standalone governance file located.

---

**CO-19 — Services Shell: Inspector Proportional Width & List Conformance**
*Status: RATIFIED — back-filled from shipped code · Historical file: `CO-19-services-layout.md` · Originally shipped: `b2b-web-v2` commit `f4efe6d`, back-filled 11 Jul 2026*

Doc-only back-fill (per `remediation-protocol.md` §2 tiering) — the token and behavior already governed in shipped code; this closed the audit-chain gap CO-20's pre-implementation audit surfaced.

- **R1:** `inspector-md-width = clamp(440px, 40%, 760px)` — single shared token for Inspector width on every B2B Web surface. Later consumed directly by CO-20 for native-Expanded (unit-normalized `dp`/`pt`).
- **R2:** filter bar wraps to two rows rather than horizontal-scroll/truncate at narrow Inspector-open widths.
- **R3:** no empty-state Inspector panel occupies layout width when nothing is selected.
- **R4:** list collapses to single SERVICE column when Inspector is open.
- Reference implementation: **Clientele** (`apps/b2b-web`) — canonical for master-detail; every future master-detail surface, web or native, conforms via this document.

---

**CO-20 — Native Window-Class & Adaptive Shell**
*Status: RATIFIED — Amended, Audit Resolved (§0.4) · Historical file: `CO-20-native-window-class.md` · Date: 11 Jul 2026 · Keyed off CO-19 · Re-versions `32_responsive_strategy.md` v1.0→v2.0→v2.1*

> **Ground-truth note (this consolidation):** the `/mnt/project` file mount for CO-20 is a snapshot that predates its own §0.4 resolution below — it still shows the completion-status audit as "PENDING." The content in this entry reflects the current, live, audit-resolved version — see §4.5.

**§0 Amendment history:**
1. **Path correction** — Seat Command's real folder is **`apps/mobile-biz`**, not the drafted `apps/seat-command`. Black Mirror's folder remains unconfirmed (don't assume `apps/black-mirror`).
2. **CO-19 provenance** — back-filled as its own document (§2.3 above), previously only a bare commit.
3. **§0.4 Audit resolved:** verdict = **2 of 11 cells COMPLETE** (Shell, M-01 Identity); **M-02–M-10 NOT-STARTED**. Root cause: two unrelated Seat Command build efforts existed. The "11 cells / 4 green gates, code-complete" record described a **backend-first NestJS effort** (`feat/b2c-mobile-autobuild`, commits `eb050e4`/`feee391`/`4be8484`/`8dccd4f`, all 27 Jun 2026) **deliberately deleted** (`d14c999` — "remove legacy app ahead of Seat Command UI-first rebuild") before the current session's rebuild began; its backend modules sit dormant on disk, untouched since `f03f30b`. The "13 cells / 4 gates" figure traced to a differently-numbered **archived branch set** (`archive/001-app-shell`…`archive/010-settings`, duplicate step numbers) from the same abandoned lineage — meaningless for this mock-only Expo UI pass. Real signals (`mobile-biz` typecheck, `gates.mjs`) both pass. **Reframes the retrofit as an integration, not a retrofit-on-finished-code:** only 2 cells needed shell integration, at the earliest possible point.

**Rulings R1–R10 (canonical, binding on both native apps):**
- **R1** No new nav components — Compact=`MobileNav`, Medium=`Sidebar`-collapsed(="Rail"), Expanded=`Sidebar`-expanded, detail=`Inspector`. One `AdaptiveShell` orchestrator.
- **R2** Native uses Material Window Size Classes in **dp** (Compact `<600` / Medium `600–839` / Expanded `≥840`); web keeps its CSS-px matrix (768/1024/1280). Invariant = escalation order + component set, not pixel equality.
- **R3** B2B/B2C asymmetry mandatory — full three-tier + persistent Inspector = Seat Command only; **Black Mirror ships Compact+Medium only**, Expanded = centered reading layout, no Inspector/Sidebar-Expanded/Zones.
- **R4** Master-detail = Inspector only, reusing the CO-19 `inspector-md-width` token — no new split-view primitive.
- **R5** IA invariant — same Cognitive Zone (`THE DESK`/`GROWTH`/`MANAGE`) on phone, tablet, web; Compact's "More" opens the identical Zone-grouped index.
- **R6** Window class keys off **app-window width (dp)** via `useWindowDimensions()` — never device model, never `Dimensions.get('screen')`; re-derives on rotation, fold/unfold, iPad Split View/Slide Over.
- **R7** Hover→touch affordance map mandatory — no action/information reachable only via hover on native; the Rail renders **expand-on-tap**, not a hover port.
- **R8** Density preserved — reject "table→cards"; dense tables render as **list + Inspector**.
- **R9** Inspector **persistent** on native-Expanded (real-estate budget browser-in-tablet lacks); overlay/sheet on Medium; pushed route, no Inspector, on Compact. The one sanctioned structural web/native divergence.
- **R10** Apple Pencil maps to exactly one affordance: **Digital Signature Capture** (Seat Command P1). No Pencil elsewhere.

**§8 Sequencing (current):** step 0 (audit verdict) — **RESOLVED**. Step 1 (`32` v2.1) — done. Step 2 (`packages/shell-kit` scaffold) — unblocked now. Step 3 (Shell+M-01 retrofit, 2 cells) — unblocked now, do before M-02 starts. Step 4 (M-02–M-10, 9 cells) — build **directly against** the adaptive-shell contract from inception, no retrofit pass needed. Step 5 (Black Mirror retrofit) — whenever that build starts. Step 6 (preference persistence) — non-blocking.
**Separate finding, not gating this CO:** `apps/api` tsc broken repo-wide (`TS5102`) — zero coupling to mobile-biz, flagged for separate tracking, unresolved.

---

**CO-21 — Chairside POS Peripheral Hardware Posture**
*Status: RATIFIED — Amended · Historical file: `CO-21-pos-peripheral-hardware.md` · Date: 12 Jul 2026*

**§0 Amendment:** original R3 didn't attribute Gulf countertop hardware to **PayTabs** specifically, and omitted the Ecosystem/V3+ "Hardware on us" waiver. Both corrected below — no ruling reversed.

- **R1** HID barcode scanner: **included, zero-adapter** — keyboard-emulation input, no vendor certification list, no pairing UI (Bluetooth pairing is OS-level).
- **R2** Cash drawer: **included, generic trigger only** — auto-fires on Cash-tender confirm; manual no-sale open is audit-logged (`hq.audit.checkout.drawer_opened_no_sale`). No payment data crosses this path.
- **R3 (amended)** Vendor payment-terminal BYO, **Gulf: excluded** — arbitrary third-party terminals stay banned (token-boundary rule, SAQ-A PCI scope). The countertop terminal TimeTribe already sells is **PayTabs-issued hardware**, not BYO — that's the sanctioned path. **Pricing:** SoftPOS free every tier; mPOS AED 549, countertop AED 1,399 one-time — **but V3+ band or Ecosystem tier gets 1 terminal per Studio location fee-waived** ("Hardware on us"; 6-month cancellation clawback; 2nd+ unit needs HQ approval).
- **R4 (amended)** Vendor payment-terminal BYO, **non-Gulf: no change** — standard hardware is **Adyen-issued**; same waiver structure applies. `spec.md` §11's **R-V6** (bounded BYO-estate exception for enterprise portfolios with a standing acquirer relationship) stays open, owned by the payments cell — not resolved by this CO.

---

**CO-22 — Single-Pane Content Max-Width — Native Medium/Expanded**
*Status: RATIFIED — full text restored, see §4.10 · Date: 12 Jul 2026 · Keyed off CO-20, extends Part II, does not amend R1–R10 · Re-versions `32_responsive_strategy.md` v2.1→v2.2 — see §4.10 for a version-collision flag against a second, independently-authored v2.2*

Trigger: screenshot evidence — Settings screen at large window width: segmented toggles read correctly, but full-width action controls (Simulate session expiry, Sign out) and list-style rows stretch edge-to-edge across the entire window, breaking visual hierarchy.

- **New §14, R11 — Single-Pane Content Width.** A third content pattern alongside shell chrome and list+Inspector master-detail: standalone single-column screens (Settings, forms, utility sub-screens). Token `single-pane-max-width = clamp(480px, 60%, 720px)` — same file as the window-class boundaries and `inspector-md-width`. Primitive `SinglePaneContainer` in `packages/shell-kit`.
- **Basis/alignment:** content area after the nav primitive; **left-aligned**, not centered — consistent with how Console/Clientele already sit relative to the Rail/Sidebar.
- **Compact unchanged** — full-bleed is correct native UX below 600dp.
- **What's constrained is the container, not each control** — a full-width button or list-row *within* `SinglePaneContainer` is correct, standard behavior; no per-component width logic introduced.
- Retrofit target: Settings (`mobile-shell`, built). Inception scope: any future cell's standalone single-column screen.
- **Real-world status:** this retrofit was never actually implemented until a live-app report ("button getting full width [at Expanded], not looking good") led back to this ruling — see `apps/mobile-biz/docs/prompts/ui-pass/GATE-LOG.md`'s CO-22 entry for the build record, commits `547b452`/`c98666c`.

---

**CO-23 — Schedule Window-Class Conformance (Inspector) & In-Screen Persistent Action Toolbar**
*Status: RATIFIED — full text restored, see §4.10 · Date: 12 Jul 2026 · Keyed off CO-20 R9, CO-22 · Re-versions `32_responsive_strategy.md` v2.2→v2.3 (new §15, R12)*

Trigger: mobile Schedule ("Today") at Expanded window class — New Appointment rendered as a bottom sheet identical to Compact, never the persistent Inspector CO-20 R9 already mandates at Expanded; Scramble was reachable only via an undiscoverable FAB long-press with no persistent toolbar even though width allowed one; web's equivalent Schedule toolbar (Book/Scramble Launch/Squeeze-in/Block) is always visible.

- **R9-applies (finding, not a new rule):** the Blade (mobile create-flow) is Inspector-class content per existing CO-20 R9. Expanded → persistent side-by-side Inspector (`inspector-md-width`, CO-19). Medium (overlay/sheet) and Compact (bottom sheet) were already correct, unchanged.
- **New R12 — In-Screen Persistent Action Toolbar.** A fourth content pattern. Medium/Expanded: a cell's primary/secondary actions render as a persistent, directly-tappable row (`ScheduleToolbar`: New Appointment/Scramble Launch/Squeeze-in/Block). Compact: existing FAB+long-press accelerator unchanged, plus an additive discoverable non-gesture path — no ratified Compact mechanic reversed. `Command` palette never ported to native (confirmed N/A, desktop-keyboard idiom).
- Toolbar is cell-owned (action set is cell-specific), not a shared shell-kit primitive — only the pattern (Compact-discoverable / Medium-Expanded-persistent) is shared canon.

---

**CO-24 — Quantum Schedule Ground Truth**
*Status: RATIFIED — full text restored, see §4.10 · Date: 12 Jul 2026 · Governs `mobile-calendar` (M-02) · Amended by CO-25 (R10 only) — R1–R9, R11 stand as below*

Trigger: `mobile-calendar` was built referencing a thin, inaccurate stand-in package (`@b2b/quantum-calendar-web`) instead of the real canonical backend `quantum-scheduling/quantum-schedule`. Full reconciliation, not a patch.

- **R1** Backend source corrected: `quantum-scheduling/quantum-schedule`, not `@b2b/quantum-calendar-web` — retired everywhere.
- **R2** Lexicon corrected: **no "Cart" in Schedule** — that's POS vocabulary. Create-flow = **The Blade**; atomic state = **BookingPayload**; commit = `confirmBooking`.
- **R3** Status model corrected: facet-based — `AppointmentStatus` (lifecycle machine) + orthogonal `paymentFacet` + `arrivalFacet`. Replaces an invented flat `EntryStatus` enum.
- **R4** Mechanics corrected: Scramble = dedicated `scrambleLaunch` mutation (not a Hold→confirm chain). Cancel = fee-preview-gated (`cancelPreview` → acknowledged `cancelAppointment`), not simple-reversible. Reschedule requires `version` (optimistic concurrency) + mandatory `seatId`; new `STALE_VERSION` failure mode.
- **R5** Squeeze-in corrected to the real `squeezePreview`/`commitSqueeze` engine (candidate ranking + Ghost ripple + displaced disclosure) — an earlier invented "tight-gap tap" mechanic and `SQUEEZE_NO_ROOM` error are retired; empty candidates is an empty state, not an error.
- **R6** Arrival Queue corrected to `getArrivalQueue`/`onArrivalQueue`/`transitionAppointment(CHECK_IN)`. **`waitBand` displays by default, never a ticking countdown** — canonical rule.
- **R7** Park Shelf corrected to `listParkedPayloads`/`parkPayload`/`resumePayload`/`discardPayload`.
- **R8** New mobile addition: Join Waitlist (single mutation, lightweight, every window class). *(Originally mischaracterized Waitlist Sniper as Owner/Manager-scoped — corrected by CO-25: canonical role table scopes Waitlist to RECEPTIONIST.)*
- **R9** New mobile addition: filter row (Seats/Status/Category+Tags), touch-adapted, binds `getTimeline`'s existing `filters` parameter.
- **R10** *(superseded by CO-25 below)* — originally excluded Entourage authoring, Series create/edit, Command HUD at every window class.
- **R11** Lifecycle transitions added: native `CHECK_IN`/`START`/`COMPLETE`/`NO_SHOW`; `COMPLETE`'s payment hand-off stays M-04 POS territory, unchanged.

---

**CO-25 — Expanded-Tier Feature Parity (amends CO-24 R10)**
*Status: RATIFIED · Historical file: `CO-25-expanded-tier-parity.md` · Date: 12 Jul 2026 · Amends CO-24 R10 only — R1–R9, R11 unchanged*

Market-validation request ("cover all web scheduler features into mobile") needed precision: real-world reception runs on iPad (Expanded window class), not phone-in-pocket. Every competitor surveyed (Phorest, Zenoti, Meevo, Boulevard) draws the same hard line between a full tablet reception terminal and a deliberately-reduced phone companion.

- **Ruling:** CO-20's window-class system is the right tool. Expanded = "iPad at reception." **Three items move from excluded-everywhere to Expanded-only:** Entourage authoring (`createEntourage`, `previewEntourageMove`, `commitEntourageMove`, `explodeMember`), Recurring Series create/edit (`createSeries`, `editSeries`), Waitlist Sniper offer-response (`respondWaitlistOffer`) — all hosted in the shell Inspector, same pattern as The Blade (CO-23 R9). Still excluded at Compact/Medium.
- **`configureWaitlist`** (Waitlist Settings — mode/fanout/window, gated `WAITLIST_MANAGE`) stays excluded at **every** window class — genuinely a configuration action, not day-to-day Receptionist work.
- **Unchanged, still excluded everywhere:** Command HUD (`parseCommand`, NL/Bedrock — zero market evidence for NL input on any mobile form factor), resize/price-override/add-Service-mid-visit (conservative default, revisit candidates, not permanent bans).
- **Ground-truth flag:** the exact claim gating `respondWaitlistOffer` isn't fully certain from the available architecture excerpt — IDE agent confirms against the live schema before binding, don't assume a claim name.

---

**CO-26 — Web Touch & Kiosk Compliance**
*Status: RATIFIED · Historical file: `CO-26-web-touch-kiosk-compliance.md` · Date: 12 Jul 2026 · Keyed off CO-20 · Re-versions `32_responsive_strategy` v2.1→v2.2*

CO-20 scoped its hover→touch doctrine to Native only, on an implicit "Web=mouse, Native=touch" assumption that doesn't hold — many Studios run the Console on a touchscreen kiosk at the front desk, same DOM/CSS, no reliable hover state.

- **R1** Web is not exclusively mouse input — kiosk deployment is first-class, assumed possible from this CO forward.
- **R2** **44×44px** minimum hit area binds on Web itself (not just as a Native translation target); trigger event is `pointerdown`.
- **R3** CO-20's R7 hover→touch map is now **also binding on Web** — identical map, same declared touch-safe equivalents required.
- **R4** Drag-and-drop must be **pointer-generic** (mouse+touch identically) — native HTML5 DnD alone is insufficient; Schedule's reschedule gesture specifically flagged.
- **R5** Kiosk terminals need a fast, visible **session-switch** affordance (new gap, not previously specified); Command HUD needs an on-screen-keyboard fallback.
- **R6** Doctrine unification, not a new tier — a kiosk is a Web-Expanded/tablet-landscape window class **with touch input**, not a new window class. Input modality and window class are orthogonal.
- **Retrofit status:** Design DNA §8c already patched (forward rule active now). **~13 already-delivered B2B Web cells need a retroactive compliance pass — not yet actioned**, queued one cell at a time or bundled.

---

**CO-27 — Role-Gated Navigation Matrix & Member/Role Identity Surfacing** *(renumbered — filed on disk as `CO-26-role-nav-matrix.md`, see §4.1)*
*Status: RATIFIED — renumbered, amended · Date: 12 Jul 2026 · Governs `mobile-shell` directly*

**§0 Amendment (13 Jul 2026):** the historical file's §7 Addendum validated Owner/Manager's bar as "3 primary tabs (Today/Console/Clientele) + More — unchanged, this was already correct." User directive: use the full 5-tab platform-conventional ceiling instead of stopping at 4. **Attendance promoted from the More hub to a 4th primary tab for Owner/Manager** (the only roles the Addendum's >5-grant/static-split branch governs) — bar is now Today/Console/Clientele/Attendance/More, 5 tabs. Every other role's bar (already flat, ≤5 grants, governed by a different branch of the same rule) is unaffected — confirmed unchanged live. Implemented generically in `nav-config.tsx` (`attendance`'s `placement` flipped to `'tab'`; `visibleTabs()`'s static branch sorts `more` last regardless of array position, so no other role's item order shifts).

Live-app report (original ruling, 12 Jul 2026): every role saw an identical nav shell instead of a job-appropriate one, plus a "Member vs Owner" naming confusion.

- **Confirmed already correct, not touched:** the `RoleId` enum (7 roles, byte-identical everywhere) and `Session.memberId` (already modeled, already separate from `role`).
- **Gap A (real):** the nav-gating *mechanism* was sound — routes hide per `permissionKey`, "hidden not disabled" — but no file anywhere specified which `permissionKey` each tab needs or which roles hold it. **Supplied now:** full role × destination grant table (Today, Console, Clientele, Inventory, Attendance, Route, Settings) across all 7 roles.
- **Gap B (real):** the word **"Member"** never appeared in mobile UI copy. Studio-Context switcher copy corrected to name it explicitly (e.g. *"Switch Studio (for this Member)"*) — a copy fix, not a redesign.
- **Notable scope calls:** Console/Clientele hidden from Receptionist/Stylist-tier (compensation and PII scope). Inventory scoped Owner/Manager/Receptionist. Attendance universal except Accountant. Route scoped Owner/Manager/Seat-tier, hidden from Receptionist. **Accountant gets Settings only on mobile — a decisive, named scope call, not an oversight** (Payroll/ledger stays a web-only surface).

---

**CO-28 — Window-Class-Conditional Tab Label (Today / Schedule)** *(renumbered — filed on disk as `CO-27-window-class-tab-label.md`, see §4.8)*
*Status: RATIFIED — renumbered · Date: 12 Jul 2026 · Governs `mobile-shell` (nav-config label resolution) directly · Keyed off CO-20 R5, CO-23, CO-25 — does not reopen any of them*

Trigger: at Expanded, mobile shows near-full scheduler parity with web (CO-25) — the tab should say "Schedule," matching web, not "Today." Cross-check found a real, pre-existing inconsistency: `32_responsive_strategy` states the bottom-bar label as flatly "Schedule," while `mobile-shell`'s actual generator prompt uses "Today" — tracing back to a CO-20-era Compact-specific correction that never got reflected back into the canon doc.

- **Compact:** **"Today"** — unchanged, deliberate. At Compact the screen genuinely shows a narrower, focused single-day view (sticky now-line, "Today" FAB, continuous scroll as date-nav) — a real, justified UX distinction, not an arbitrary label choice.
- **Medium / Expanded:** **"Schedule"** — matches web's own label exactly, justified because the content genuinely broadens toward web parity at these window classes (full Canvas, and per CO-25, Entourage/Series/Waitlist-offer-response at Expanded).
- Label-only rule — does not change CO-27's grant matrix (who sees this destination), CO-20 R9 (Inspector presentation), or CO-23 R9 (Blade/Inspector authoring pattern). Route (`/(tabs)/today`) stays the stable identifier; no path rename.
- **Implementation:** `nav-config`'s `today` item gains an optional `labelKeyExpanded` field (this item only, not schema-wide) — read by `NavRail` (Medium) and `Sidebar` (Expanded) in preference to `labelKey`, falling back to it otherwise; `TabBar` (Compact) is untouched and always reads `labelKey`.

---

**CO-29 — Single-Pane Content Width, Pre-Auth Screens (Sign-in, MFA, Bootstrap)**
*Status: RATIFIED — new this filing · Date: 12 Jul 2026 · Governs `mobile-shell` pre-auth surfaces · Keyed off CO-22 — extends its principle to a case its own basis couldn't cover, does not reopen CO-22's Settings/post-auth ruling*

Trigger: screenshot evidence — Sign-in at Expanded (1024×939, CO-20 matrix, not a distinct "laptop" breakpoint) showed the Email/Password fields and Continue/Apply/Emergency-Code buttons stretching to the full viewport width — CO-22's exact failure mode, but pre-auth was never in CO-22's own reach: that fix was anchored to "the content area after the nav primitive," which doesn't exist before sign-in.

- **Reuse `SinglePaneContainer` and `single-pane-max-width` unchanged — no second token.** At Medium/Expanded, Sign-in, MFA enrol/verify, biometric enrol/verify, session-bootstrap (skeleton), and the expired-session re-auth interstitial wrap in the same container Settings already uses. Compact unchanged (full-bleed, as it already is).
- **What's constrained is the container, not the buttons — same CO-22 principle, restated because it matters here too.** A full-width primary button inside a properly-sized container is correct, standard auth-flow behavior; this is not "buttons are too big," it's "the container is too wide."
- **Alignment differs from CO-22, for a stated reason, not an inconsistency:** CO-22 left-aligns to stay flush with the nav rail; pre-auth has no nav rail to stay flush with. This case **centers** the container instead — near-universal convention for auth/onboarding on tablet-class devices.
- **Scope:** Sign-in, MFA enrol/verify, biometric enrol/verify, session-bootstrap, expired-session re-auth, all at Medium/Expanded. Out of scope: Settings or any other post-auth single-pane screen (CO-22's left-aligned rule stands, untouched); a new token/component; any change to `32_responsive_strategy.md` (this is cell-scope, not cross-cutting).

---

**CO-30 — Service Delivery Mode & Route Tab Capability Gating**
*Status: RATIFIED — new this filing · Date: 13 Jul 2026 · Governs `mobile-shell` (nav-config, Route row) · `studio-settings` · `roster-compensation`/Seat profile · Keyed off CO-27's role×destination matrix — extends it, does not reopen it*

Trigger: Chief Architect session — tablet-reception validation surfaced that Route's CO-27 grant is role-only (Owner/Manager/Senior/Junior Stylist/Freelancer ✓). Role eligibility answers "can this job function ever do mobile service," not "does this specific Studio/Seat actually offer it." Confirmed via project-knowledge search: "mobile-service seats," "mobile Service providers," and "mobile-service travel buffers" are referenced across M-07 (Mapping), M-05 (Attendance's 100m-GPS rule), and the Maps adapter — but no canonical field anywhere defines which Studios or Seats they apply to.

- **Two new fields, same multi-value pattern as `studio.verticals[]`:**
  - `Studio.service_modes[]: (IN_STUDIO | MOBILE)[]` — business-model flag, Studio-scoped, set in Studio Settings/onboarding. Defaults `[IN_STUDIO]` on Studio creation (nothing mobile-visible until explicitly opted in). Distinguishes studio-only / home-only / both at the Studio level; also the field a future marketplace filter would read (not built this filing — flagged for that cell's own trigger).
  - `Seat.mobile_enabled: boolean` — per-Seat capability toggle, Owner/Manager-write only. Constraint: can only be `true` if the parent Studio's `service_modes[]` includes `MOBILE`; UI disables the toggle otherwise. Defaults `false`.
- **Route tab visibility, amended:** `role-eligible (CO-27 §2 matrix) AND seat.mobile_enabled === true`. CO-27's role column is unchanged — this adds an AND-condition, it doesn't reopen who's eligible by role.
- **Freelancer's "(primary)" bar-promotion is no longer unconditional.** A Freelancer with `mobile_enabled === false` (chair-only freelancer) falls back to Today as primary — no role gets a hardcoded bypass of the capability check, Freelancer included.
- **Not built this filing:** `mobile-mapping` (M-07) itself remains NOT-STARTED per current build state; this rules on the gating contract it must honor once built. Studio Settings and Seat-profile UI surfacing land on their own next surface trigger (`studio-settings`, `roster-compensation`) — same deferred-implementation pattern as CO-21 R1/R2.

---

**CO-31 — Stylist View-Scope: Own + Assisted Clamp (Quantum Schedule)** *(renumbered — collided with this file's own CO-30 "Service Delivery Mode," see §4.11)*
*Status: RATIFIED (ruling) — code fix PENDING · Date: 13 Jul 2026 · Governs `quantum-scheduling/quantum-schedule` backend (`apps/api/src/b2b-quantum-scheduling`), B2B Web scheduler (`apps/b2b-web/lib/scheduler`, `.../components/screens/scheduler`), B2B Mobile `mobile-calendar` (M-02) · Keyed off CO-24 (Quantum Schedule Ground Truth) — surfaced as a follow-on during M-02 v2 spec reconciliation, from a separate session working in the `timetribe-platform` monorepo*

Trigger: while reconciling M-02 v2's mobile-calendar spec against ground truth, found that both the canonical `quantum-schedule/spec.md` ("SENIOR_STYLIST: own + assisted operation"; "JUNIOR_STYLIST: own lane operation, no resize") and mobile's inherited copy describe Stylist-tier roles as view-restricted, language paralleling FREELANCER's own-lane silo. Direct inspection of the shipped backend and B2B Web scheduler found this was never implemented: the only view-restriction mechanism anywhere in the stack, `ownLaneOnly` (`types.ts:376`), is wired exclusively to `FREELANCER_SEAT_ID` (13 call sites in `scheduler-surface.tsx`), mirrored server-side only for `user.role === 'FREELANCER'` in `getTimeline` (`b2b-quantum-scheduling.service.ts:250`) and `assertFreelancerScope` (:68-78). Grepping the full backend + web scheduler tree for "assist" returns zero matches — the "assisted" concept named in spec has no data-model representation at all (Appointment carries a single `seatId`; confirmed against `core-services/002-appointment-orchestration/data-model.md`). Net effect, live today: every Junior and Senior Stylist sees every other stylist's full timeline, including Client name/contact — medium-high PII per this same spec's §10 classification, residency-pinned under PDPL/GDPR/UK GDPR/US state/AU Privacy Act/PIPEDA. Escalated to the user rather than silently resolved either direction (live-system compliance question, not an architecture-derivable fact) — ruled: **spec intent is authoritative; today's shipped full-Roster access for Stylist tiers is a gap, not a valid baseline.**

- **R1 — Stylist tiers are view-scoped to own + assisted, not full-Roster.** SENIOR_STYLIST/JUNIOR_STYLIST see only: Appointments on their own Seat, plus Appointments they're explicitly tagged as assisting on. Full-Roster view (today's shipped default) is retired for these two roles across every surface — web, mobile, any future surface reusing this backend. OWNER/MANAGER/RECEPTIONIST unaffected.
- **R2 — "Assisted" needs a real data-model field this CO does not fully spec.** No `assistingSeatIds`-equivalent exists today. Minimum shape: a per-Appointment, multi-value assisting-Seat reference, settable by any role that can already write that Appointment. Exact assignment UI, mutability post-creation, and Junior self-assignment are **open — flagged to Product/Design.** Build `own`-only first; add `assisted` once that field lands.
- **R3 — Backend enforces, not just UI.** Mirror the Freelancer pattern: a server-side row filter in `getTimeline` plus a `capsForRole` clamp analogous to `ownLaneOnly`. A client-only mask is insufficient — the wire already carries the full unfiltered timeline regardless of role, so UI-only hiding leaves the PII exposure intact at the network layer (worse on mobile than web — device loss/screenshot risk). No new AppSync surface.
- **Code impact — "own"-half IMPLEMENTED 13 Jul 2026 in the monorepo (R2's "assisted" half still not started).** Fixed in both of this ruling's currently-disconnected subsystems: **`apps/api`** (the real backend mobile will eventually bind to) — `signSessionToken` now resolves `seat_id` via the pre-existing `SEAT_BY_USER_ID` map instead of the user's own `user_id` (this was ALSO silently breaking the FREELANCER clamp before now, not just leaving Stylists unclamped); `getTimeline`'s row filter and the renamed `assertSeatScope` (was `assertFreelancerScope`) now cover SENIOR_STYLIST/JUNIOR_STYLIST via a new `SEAT_SCOPED_ROLES` set. **`apps/b2b-web`** (the currently-live, self-contained mock the web app actually renders — confirmed via code trace that it does NOT call `apps/api` at all today, entirely separate seat-ID schemes) — `ScheduleCapabilities` gained `ownAndAssistedOnly`; `resolveOwnSeatId(role)` + per-tier Seat-ID constants added; `scheduler-surface.tsx`'s 13 `ownLaneOnly` call sites generalized. **Live-verified** in-browser: both Stylist tiers correctly clamped (Appointments + Arrival Queue — the latter a real gap found only by live-testing, `ArrivalEntry` carries `seatName` not `seatId`); Manager/Owner/Freelancer confirmed regression-free. `tsc --noEmit` clean on both apps. **Still pending:** the "assisted" Seat inclusion (R2) — no data-model field exists in either subsystem yet.
- **Consequence for M-02 v2 Phase 1 — sequencing effectively resolved in favor of (a).** (a) Block Phase 1 until the b2b-web backend fix lands, mobile reuses corrected `getTimeline` directly, zero mobile-side special-casing (matches this rebuild's "no mobile-invented scoping" discipline, CO-24 R1) — **now satisfied, the backend fix has landed.** (b) (the interim mobile-side clamp alternative) is now moot for the "own" half; may still matter later if `assisted` (R2) lands on mobile before web, though R2 is unstarted on both sides today.
- **Follow-ups:** ~~backend row-filter + write-permission clamp~~ — **done** (write-permission clamp deliberately untouched — CO-31 governs view-scope, not the separate matrix `assertPermission` already enforces). Prisma schema field for assisting-Seat (R2) — **still open**, pending Product/Design. ~~`capsForRole`/`ScheduleCapabilities` clamp in `apps/b2b-web`; apply at all 13 call sites~~ — **done**, plus the Arrival Queue gap. `mobile-calendar/spec.md` and `quantum-schedule/spec.md` — updated in the monorepo to cite CO-31 (this repo's renumbering, resolved via §4.11) and to reflect the "own"-half landing.

---

**CO-32 — Add-on Catalog Cascade: MULTI_LOC, Bundle Rename, Utilisation Guard SKU**
*Status: RATIFIED — new this filing · Date: 13 Jul 2026 · Governs Pricing Data v2.0 §4.3/§4.5/§5.1/§5.2, entitlements SKU registry · Keyed off CO-17 (D9 lineage) and `pricing-plan-*-v1_6.md` §10 (D5, D9 — ratified there per-region, never cross-logged into this ledger)*

Trigger: Chief Architect session (13 Jul 2026), user directive to validate and act on findings from the prior session's add-on-catalog audit. Cross-referencing Pricing Data v2.0's add-on catalog against all 7 regional `pricing-plan-*-v1_6.md` files (via `project_knowledge_search`) surfaced three uncascaded items, none previously logged in this ledger.

- **R1 — MULTI_LOC eligibility correction cross-logged.** Confirms `pricing-plan-*-v1_6.md` §10 D9 (already ratified per-region, dated at each region's v1.6 filing): `MULTI_LOC.eligible_tiers` = `[ECOSYSTEM]` only, Tribe removed (was Tribe+Ecosystem). Pricing Data v2.0 §4.5 still shows Tribe ✓ — confirmed stale via direct read, cascade required.
- **R2 — `BUNDLE_ENTERPRISE` → `BUNDLE_ECOSYSTEM` cross-logged, migration path specified.** Confirms §10 D5 (already ratified per-region). Pricing Data v2.0 §5.2 retains the legacy code by its own explicit note ("does not rename... to avoid migration risk on bundle materialization logic"). Ruling: display name "Ecosystem Bundle" is final (already live everywhere); SKU code migrates dual-compat — entitlements service accepts both `BUNDLE_ENTERPRISE` (existing subscriptions, read-only) and `BUNDLE_ECOSYSTEM` (all new writes) until a dedicated migration cell retires the legacy code. Resolves the original engineering-risk concern instead of overriding it.
- **R3 — `UTILIZATION_GUARD` formalized as a canonical SKU code; tier-eligibility OPEN.** Referenced by identical price (129 AED/mo, consistent regional equivalents — 129 SAR, £29, $35) across all 7 `pricing-plan-*-v1_6.md` §3 lines and the Ecosystem Bundle's 7-module composition, but absent from Pricing Data v2.0 §5.1's registry entirely — no SKU code exists today. Code + price ratified: `UTILIZATION_GUARD`, 129 AED/mo (+ regional equivalents), unlocks downgrade-prevention workflow (exit survey, automated waitlist deployment for late cancellations) — description carried over from the superseded `timetribe_addon_strategy.md`, the only source with a capability description on record. **Tier-eligibility NOT ratified — conflicting signals found:** the superseded addon-strategy doc filed it under Enterprise-tier (≈ today's Ecosystem) modules; but current `pricing-plan-*-v1_6.md` §3 inline lists tag every Ecosystem-exclusive item explicitly (e.g., "Yield Shield 5T 199 (Eco)"), while Utilisation Guard carries no tag at all — the same untagged pattern used for confirmed Tribe+Ecosystem items (Lookbook, Console Telemetry Pro, Advanced Memberships, Revenue Artillery Pro). Both readings are defensible; not enough here to pick one without guessing. Flagged for Growth/Founders or direct Chief Architect confirmation — same posture as CO-17's floor values.
- **R4 — Ecosystem Bundle composition corrected to 7 modules.** Pricing Data v2.0 §5.2 lists 6 (`YIELD_SHIELD_5T` + `REVENUE_ARTILLERY_PRO` + `WATCHTOWER_PRO` + `AI_CONCIERGE` + `CORPORATE_MEM` + `TELEMETRY_EXPORT`); `pricing-plan-uae-v1_6.md`'s own bundle description already lists 7, adding `UTILIZATION_GUARD` (R3). Price unchanged — 1,299/mo already prices for 7 per the live v1.6 doc; this is a registry catch-up, not a re-pricing. Depends on R3's tier-eligibility resolving to Ecosystem-inclusive before this closes cleanly — if R3 resolves to Tribe+Ecosystem, the Growth Bundle composition needs the same review.
- **Follow-ups:** Pricing Data v2.0 §4.3/§4.5/§5.1/§5.2 need the actual file edit — this entry is the ruling; the doc itself is a separate cascade target outside this repo, not owned here. R3's tier-eligibility — open, blocks R4's clean close. No code impact — this CO corrects a data/documentation registry, not a build gate.

---

**CO-33 — Add-on Feature-Gate Definitions (Console Telemetry Pro, Revenue Artillery Pro, Lookbook Accelerator) & `AI_CONCIERGE` Naming Collision**
*Status: RATIFIED — new this filing; gates not yet built, see Follow-ups · Date: 13 Jul 2026 · Governs pkg B2B-W-06 (`35_watchtower_hud`, `47_analytics_architecture`), pkg B2B-W-05 (Marketing Console), B2C Lookbook spec (`spec8-B2C-agent-assignments`, B2C Lexicon), entitlements SKU registry, Cognito B2B token contract (`active_addons[]` gate targets) · Keyed off CO-11 (SKU/entitlements re-scope) — extends it to three SKUs it never defined a gate for*

Trigger: Chief Architect session (13 Jul 2026), user directive to validate and act. Cross-referencing three add-on SKUs' stated "unlocks" (Pricing Data v2.0 §5.1) against the actual build specs for each underlying feature found the build spec already describes the full capability as included and ungated, with zero mention anywhere of a Pro-gated subset — the SKU exists in the pricing catalog with nothing behind it. A fourth SKU (`AI_CONCIERGE`) collides in name with an already-canonical, differently-scoped platform feature.

- **R1 — Console Telemetry Pro gated on the existing 3-layer boundary; no new component invented.** Docs 35/47 + pkg B2B-W-06 spec one ungated tri-layer Console (Layer A Ops HUD, Layer B Executive Overview, Layer C Ledger). Ruling: Tribe/Ecosystem base = Layer A only, on-demand refresh (page load / manual), no live subscription. `CONSOLE_TELEMETRY_PRO` unlocks Layer A promoted to live (`onPulseUpdated` AppSync subscription, 5s debounced fan-out — already spec'd in pkg B2B-W-06, now gated behind this SKU) + Layer B + Layer C (incl. CSV/PDF export). Solo: no Console access — below both the tier floor and the SKU's own eligibility (Pricing Data v2.0 §4.5).
- **R2 — Revenue Artillery Pro gated on channel/execution scale, per already-stated platform doctrine.** `SaaS_Monetization_Strategy_Audit.md` §1: "Intelligence models... are base-level inclusions. Monetization occurs at execution and orchestration scale" — this ruling operationalizes that sentence for the first time. Base Marketing Console (pkg B2B-W-05) = single-channel per campaign (SMS or WhatsApp or Email, studio picks one), one Ghost Writer draft per send, standard playbooks (Gap Fill / Upsell / Review Request / Win-Back) usable one at a time, no split-testing. `REVENUE_ARTILLERY_PRO` unlocks: simultaneous multi-channel fan-out per campaign, A/B draft variants, advanced Ghost Writer tone-calibration controls, and compound/saved audience segmentation beyond Sniper Scope's base filters. **"Surge Shield" (named in the SKU's own §5.1 description) is NOT ruled on — zero supporting spec found anywhere in project knowledge beyond its own name. Flagged as a prerequisite gap: needs its own spec before this SKU can build.**
- **R3 — Lookbook Accelerator gated as automation of an already-manual studio workflow; the client-facing Lookbook itself stays ungated.** All B2C-visible Lookbook infrastructure (consent-gated portfolio, Verified Results, tag-based visual search, Aesthetic Match Engine) is core platform infrastructure and is NOT, and was never proposed to become, an add-on. Only the studio-side authoring workflow is gated. Base: stylist manually uploads and hand-tags each Lookbook photo (taxonomy: technique/length/texture/finish, per B2C strategy doc). `LOOKBOOK_ACCEL` unlocks: auto-tagging via the already-spec'd Rekognition+Gemini vision pipeline (`timetribe-complete-spec-part7` §6 — today scoped only to client-side visual-search matching, this ruling extends its use to studio-side auto-tagging), automatic before/after pairing from the same Appointment's pre/post photos, and AI style-transfer preview generation.
- **R4 — `AI_CONCIERGE` (B2B SKU) renamed `AI_COMMAND`, display "AI Command."** Collision: B2C Lexicon §2.9 already canonically defines "AI Concierge" as the platform's own client-facing conversational booking agent (Vertex Gemini, core marketplace infrastructure — not studio-purchased, not gated by any B2B entitlement). The audited B2B SKU unlocks a materially different, studio-facing capability set ("AI chat, AI Ghost Writer, AI Lens") for the studio's own Console. Same name, different product — violates the lexicon discipline enforced everywhere else on this platform (CO-16, CO-27 precedent: resolve collisions by rename, not redefinition). "Command" keeps the naming family already established (Command Seat, Seat Command, Watchtower's "command HUD"). Price/tier unchanged — 249 AED/mo, Ecosystem-only. Feature scope unchanged. Naming-only ruling.
- **Follow-ups:** R1–R3 are gating contracts, not built this filing — same deferred-implementation posture as CO-30 R4 / CO-21 R1-R2; entitlement checks have no code to enforce until each surface's own next build trigger. R2's Surge Shield needs its own spec authored before any RAP build. R4's rename needs SKU-code migration — same dual-compat pattern as CO-32 R2 (`AI_CONCIERGE` read-compat, `AI_COMMAND` write-forward). Cascade targets: Pricing Data v2.0 §5.1, entitlements service, `active_addons[]` claim values (Cognito B2B token contract), Marketing Web `/pricing`, Studio Console upsell copy.

---

**CO-34 — Standard-vs-Add-on Market-Fit Correction: Closes CO-32 R3, Amends CO-33 R1/R2/R4, New Tier-Included Capability (Loyalty Ladder)**
*Status: RATIFIED — new this filing; R6 (Surge Shield) still open · Date: 13 Jul 2026 · Governs Pricing Data v2.0 §5.1/§5.2 (Utilisation Guard tier-eligibility), pkg B2B-W-06 (Console Telemetry Pro Standard scope), Yield Shield spec (`34_yield_shield.md`) + pkg B2B-W-05, entitlements SKU registry, all 7 `pricing-plan-*.md` files (UAE v1.8; KSA/UK/US/EU/AU/CA v1.7) · Keyed off CO-32 R3 (closes it), CO-33 R1/R2/R4 (amends), CO-11 (Tier-Included Capabilities model, extended)*

Trigger: Chief Architect session (13 Jul 2026), user directive to validate the Standard-tier/add-on split against market (Fresha/Booksy, both re-benchmarked this session — see UAE v1.8 §0 for a Fresha regional-pricing correction folded into that validation but not carried into this CO, since it doesn't change any platform ruling) and against internal spec ground truth. Found: (1) CO-32 R3's Utilisation Guard tier-eligibility ambiguity resolves cleanly on evidence already on file but not previously cross-checked; (2) CO-32 R3's own Utilisation Guard description (carried into CO-33's build scope) duplicates a P0 core feature; (3) CO-33 R1's Console Telemetry Pro boundary gates a Studio's own data export, off market norms; (4) CO-33 R4's AI Command rename inherited Standard-tier wording broad enough to conflict with the platform's own written AI Doctrine; (5) Yield Shield (149 T / 199 E, not previously gate-defined by any CO) bundles baseline loyalty tracking with genuinely premium demand-pricing automation, gating something Fresha/Booksy both give away free or near-free.

- **R1 — CO-32 R3 CLOSED: Utilisation Guard is Ecosystem-only, not OPEN.** Both source documents (`timetribe_addon_strategy.md`, `SaaS_Monetization_Strategy_Audit.md`) place it under an explicit "Enterprise Tier Modules" header — CO-32 R3 read this as ambiguous, but Enterprise-tier module placement is unambiguous (Pricing Data v2.0 §1.2 confirms `ENTERPRISE → ECOSYSTEM` is a straight rename). Separately: every `pricing-plan-*.md` §3 SKU table uses three tags — `T` (Tribe-only), `E` (Ecosystem-only), `T, E` (both) — and Utilisation Guard is tagged plain `E`, matching every other confirmed Ecosystem-only row, not the `T, E` pattern CO-32 R3 read it as sharing. Both signals agree; there was no genuine ambiguity, only an uncross-checked one. **CO-32 R4's dependency ("if R3 resolves to Tribe+Ecosystem, the Growth Bundle composition needs review") does not trigger — R3 resolves Ecosystem-only; Growth Bundle is unaffected.**
- **R2 — Utilisation Guard's "automated waitlist deployment for late cancellations" description (CO-32 R3, carried into CO-33's build scope) is struck — it duplicates Waitlist Sniper, a P0 core feature already live at every tier with no SKU.** `timetribe-complete-spec-part2.md` §3.1 specs instant SMS broadcast to the top 3 waitlisted Clients the moment a slot opens, first-to-reply secures it; `spec8-B2B-agent-assignments.md` Package B2B-W-02 (Quantum Calendar) has this live today via an `onWaitlistSlotOpened` AppSync subscription, gated by nothing. CO-32 R3's description was carried verbatim from the superseded `timetribe_addon_strategy.md` without cross-checking it against the calendar package — an IDE agent building from the add-on spec and one building from the calendar spec would have collided on this exact mechanism. Utilisation Guard's real, non-duplicate paid value: exit-survey capture + downgrade-prevention analytics (usage-pattern detection — declining visit frequency, spend drift — surfaced to the Owner as a proactive retention prompt). No price change (129 AED/mo + regional equivalents stands).
- **R3 — Console Telemetry Pro Standard/Add-on boundary corrected (amends CO-33 R1).** CO-33 R1 gated all of Layer C (Ledger, including its CSV/PDF export) behind the add-on. Corrected: baseline Ledger data (a Studio's own revenue/transaction line-items) and CSV/PDF export of that raw data move to Standard (Tribe+, no SKU) — gating "export my own data" checked against Booksy (all features included, one price) and Fresha (narrow, named add-ons; no data-export gate found) and came back off market norms. `CONSOLE_TELEMETRY_PRO` retains: Layer A promoted to live (`onPulseUpdated`, unchanged from CO-33 R1) + Layer B Executive Overview + the *styled* Layer C presentation layer (bento KPIs, sparkline MetricTiles, RevenueCapacityChart, StaffLeaderboard) — raw data and export already available without it. No price change.
- **R4 — Yield Shield (149 T / 199 E) gate-defined for the first time — split into tier-included tracking + SKU-gated automation.** Not previously covered by CO-33 (its scope was Console Telemetry Pro / Revenue Artillery Pro / Lookbook Accelerator only) or any other CO. Today, a Tribe+ Studio has zero loyalty program without this SKU. Ruling: Bronze→Gold loyalty tracking (points accrual, tier status, reward redemption, base-earn multipliers) becomes a new **§2.1-class tier-included capability** — Tribe+, no SKU, never in `active_addons[]` — per the same model CO-11 established for SoftPOS/Waiting Room Display/Beauty Squad. Rationale: Fresha's own help-center lists loyalty as a separate, narrower, cheaper add-on than TimeTribe's bundled tracking-plus-automation SKU; gating bare tracking is off market and works against TimeTribe's own economics (loyalty drives the repeat-visit volume the processing-fee margin depends on). Yield Shield 3-Tier/5-Tier (unchanged price) now unlocks only the demand-pricing automation engine (VIP-routing, Value Injection triggers) plus, for 5-Tier, the Platinum/Diamond tier extension — those two tiers remain fully SKU-gated, tracking and automation both. No price change.
- **R5 — AI Command Standard-tier wording corrected (amends CO-33 R4's inherited scope).** The SKU's Standard-tier description ("no AI... surfaced... at all" without the add-on) is broader than the platform's own AI Doctrine permits — `Pricing_Data_v2.0` §13's Forbidden Display Patterns explicitly bans gating "AI insights" behind add-ons ("only AI execution at scale is monetized"), and the Studio Console's existing "AI insights" surface (visible in reference product imagery) would be blocked by a literal reading of the prior wording. Corrected: Standard column now explicitly protects passive AI insights (recommendations, forecasting, anomaly flags); the add-on gate is scoped to active AI execution only (chat sessions, on-demand Ghost Writer drafting, AI Lens vision auto-tagging). Wording fix only — no price or tier-eligibility change; R4's rename (`AI_CONCIERGE`→`AI_COMMAND`) and dual-compat migration path stand as ratified.
- **R6 — Revenue Artillery Pro's Surge Shield gap (CO-33 R2) confirmed still open, with new information: two conflicting specs exist, not zero.** CO-33 R2 found no spec at all. This session found two: `34_yield_shield.md` ("Surge Shield Active," marked IMPLEMENTED — utilization<40% "Ghost Town" trigger, hardcoded value-adds) and `08_timetribe_marketing_v2.md` (marked Gold Standard Validated 100/100 — utilization<30% configurable, 48h window, owner-defined incentive library). Both real, both detailed, mutually inconsistent on trigger threshold and mechanism, filed under different module ownership (Yield Shield vs. Revenue Artillery Pro / Marketing Console). **Not ruled on here — this is a reconciliation task (which spec is the build baseline, or how the two relate), not a gap-fill.** CO-33 R2's "blocks build until authored" status is corrected to "blocks build until reconciled" — a spec exists, in duplicate, not in absence.
- **Cascade — executed this session, all 7 regions:** `pricing-plan-uae-v1_8.md` (D20–D23), `pricing-plan-ksa-v1_7.md` / `-uk-v1_7.md` / `-us-v1_7.md` / `-eu-v1_7.md` / `-au-v1_7.md` / `-ca-v1_7.md` (D17–D23, or D17–D24 where a same-document Revenue-Floor table/prose reconciliation also applied — UK, EU, CA; discovered incidentally while re-authoring these filings, unrelated to R1–R6, values unchanged). §2.1's tier-included capability table gains Loyalty Ladder in all 7 documents; §3.2 (new in the 6 non-UAE regions, corrected in UAE) reflects R1–R5 directly, never carrying the pre-correction language.
- **Follow-ups:** `Pricing_Data_v2.0` refresh (§4.3/§4.5/§5.1/§5.2, plus this CO's R1–R5) — **not done this session**, needs a full read first (only ~270 of its ~540 lines had been viewed across two sessions at time of filing); its citation of the legacy, stale `SPEC-8___SpecKit_Agent_Assignment` (dated 26 Apr 2026, predates 10+ ratified corrections including CO-32/33/34) as "canonical" should be repointed to `spec8-B2B-agent-assignments.md` in the same pass. Surge Shield (R6) needs reconciliation before Revenue Artillery Pro's `ide-backend` triggers. No code impact from R1–R5 beyond entitlement-boundary wording — same deferred-implementation posture as CO-32/CO-33; gates are contracts, not yet built.

---

## 3. Change-Order Execution Artifacts (not their own CO, cross-referenced here)

- **`pricing-cell-amendment-v1.md`** — the literal retrofit prompt applying CO-17's §1/§1.1 rewrite + D14/D15/D16 registry rows across all 7 `pricing-plan-*` files. Confirms live admission-floor figures: UAE/KSA 15,000/40,000/75,000 (AED/SAR); UK 3,200/8,500/16,000 GBP; US 4,000/11,000/20,000 USD; EU 3,600/9,500/18,000 EUR; AU 6,000/16,000/30,000 AUD; CA 5,500/14,500/27,000 CAD.
- **`seat-title-picklist-v1.md`**, **`seat-command-cell-completion-audit.md`** / **`cell-completion-audit-findings.md`** — companion documents to CO-16 and CO-20 respectively, not independently ratified change orders.

---

## 4. Governance Findings (surfaced during this consolidation)

**4.1 — CO-26 numbering collision, resolved.** Two independently-ratified documents both self-identify as "CO-26," both dated 12 Jul 2026, both status RATIFIED: *Web Touch & Kiosk Compliance* and *Role-Gated Navigation Matrix & Member/Role Identity Surfacing*. Decisive call: **Web Touch & Kiosk Compliance keeps CO-26** — it's the one `32_responsive_strategy_v2_2.md`'s own version-history changelog cites by number, and it sits in the direct CO-19→CO-20→CO-26 dependency chain. **Role-Gated Navigation Matrix is renumbered CO-27.** Its on-disk filename (`CO-26-role-nav-matrix.md`) is left unrenamed to preserve the audit chain — a header note pointing to its CO-27 identity is a recommended follow-up, not done in this filing since it wasn't requested.

**4.2 — CO-18, orphaned citation.** Referenced only by `b2b-web-mobile-parity-reconciliation-prompt.md` §Authority-order and §Step-2 as *"CO-18 (Tab: underline, label-only, sole section-nav)"* — a real, specific ruling, but no document anywhere calls itself CO-18. Same state CO-19 was in before its 11 Jul back-fill. **Recommend the identical back-fill treatment** the next time the Tab component is touched.

**4.3 — CO-22, no record.** Zero citations, zero standalone document, across every search run for this consolidation. Row reserved in §1 with status NOT FOUND rather than silently skipped or invented.

**4.4 — CO-23 and CO-24, orphaned citations.** Both cited extensively — CO-24 by name with R1, R8, R10, R11 individually referenced; CO-23 by name with R9 referenced — exclusively from within CO-25 and the parity-reconciliation prompt. No standalone source document for either was located in project knowledge. §2.3's CO-23/CO-24 entries above contain only what could be reconstructed from citation, explicitly marked as such — nothing was inferred or filled in beyond what other ratified documents state outright.

**4.5 — CO-20 repo-mount snapshot is stale.** The `/mnt/project/CO-20-native-window-class.md` file mount reflects an **earlier draft** — header still reads `Code impact: Retrofit — PENDING AUDIT`, §0 stops at item 3, §8 shows the audit as an open hard gate, and the sign-off block has no "Audit resolved" line. The live, current version of the same document (surfaced via project-knowledge search, and consistent with prior confirmed findings) carries a **§0.4 Audit resolved** section with the full two-branches root-cause account and a materially different §8. **CO-101 uses the live, resolved version throughout §2.3.** Recommend re-syncing the `/mnt/project` snapshot so future `view` calls don't retrieve the superseded draft.

**4.6 — CO-5 (HQ Lexicon v1.0), completion unconfirmed.** As of the 06 Jul 2026 report, CO-5's own Watch Register flagged it "**still unauthored**" (W-5) and the first-position blocker for every `hq web prompts` cell. Nothing in project knowledge confirms `/specs/hq-web/_master/HQ_LEXICON_v1.md` was subsequently authored. **Verify before authoring further HQ Web cells** — if unauthored, it remains the standing prerequisite CO-5 itself named.

**4.7 — CO-15 / S-11 phasing-paragraph fix not reflected in live Project Instructions.** CO-15 explicitly ordered Project Instructions' phasing paragraph rewritten away from hardcoded "Phase 1 = UAE… Phase 2 = KSA" language, to doctrine wording ("Supported regions in build; launch per-region, board-mandated, UAE first"). **The Project Instructions currently governing this conversation (labeled v3) still read:** *"Phase 1 = UAE (`me-central-1`, AED). Phase 2 = KSA (`me-central-2`, SAR, ZATCA-ready at contract level)."* Either the v3.1 rewrite never propagated to the live instructions, or the phrase was deliberately reintroduced after CO-15. **Flagged, not resolved here** — amending the live Project Instructions is outside this ledger's authority; it's the Chief Architect's document to change.

**4.8 — A second CO-27 numbering collision, undetected by this consolidation's own audit, resolved.** `docs/CO/CO-27/CO-27-window-class-tab-label.md` ("Window-Class-Conditional Tab Label") independently self-identifies as CO-27, ratified 12 Jul 2026, file mtime 23:07:08 — roughly 24 minutes before this ledger's own mtime (23:31:28). §4.1 above resolved the *original* CO-26 collision by renumbering "Role-Gated Navigation Matrix" to CO-27, but that resolution did not cross-check the freed-up number against this document, which had already claimed it independently. Corroborating evidence that the collision is real, not hypothetical: the role-nav-matrix document's own §7 Addendum (same session) reads *"Today/Schedule's CO-27 label rule is unaffected"* — citing the tab-label ruling AS "CO-27," from inside the very document this ledger renumbered TO CO-27. Two ratified documents, each unaware of the other, both landed on the same number by independent paths. **Decisive call:** Role-Gated Navigation Matrix **keeps CO-27** — already in wide use (this ledger's Master Index, its own cross-references, and its standing as the architecturally central ruling for `mobile-shell`'s nav-gating). **Window-Class-Conditional Tab Label is renumbered CO-28** (§2.3 above) — content unchanged, appended per this ledger's own "next available number, no new file" rule. Its on-disk filename (`CO-27-window-class-tab-label.md`) is left unrenamed, same precedent as §4.1. Recommend the same header-pointer follow-up §4.1 recommended for CO-26/27, now also owed to this file.

**4.9 — Repo-mount path mismatch (pre-existing, not introduced by this filing).** §5's stated hierarchy and every individual CO's "Repo mount" field — including this ledger's own (§ header) — point at `docs/change-orders/`. That directory does not exist on disk. Every actual file, including this ledger, lives under `docs/CO/` (with per-CO subfolders in some cases, e.g. `docs/CO/CO-27/`, `docs/CO/CO-26/`). This predates CO-101 — the tab-label document's own header already cited `/docs/change-orders/...` before this ledger existed — so it is a pre-existing convention/reality mismatch, not something introduced here. **Flagged, not resolved here:** renaming the real folder, or correcting every historical "Repo mount" field to say `docs/CO/`, is a repo-wide change outside a documentation ledger's authority to make unilaterally. Recommend the Chief Architect pick one — rename `docs/CO/` → `docs/change-orders/` to match the stated convention, or correct the convention to `docs/CO/` — the next time any CO file is touched.

**4.10 — CO-22/23/24 were "not found" because they were never committed — now restored; this reveals a likely `32_responsive_strategy.md` v2.2 version collision, flagged, NOT resolved.** The 12 Jul consolidation correctly reported CO-22 as not found and CO-23/CO-24 as citation-only — those documents genuinely weren't in the repo at that time; they existed only as chat-delivered files, never pushed, and in fact were **never pushed as standalone files at all** — confirmed directly against the real monorepo filesystem (13 Jul 2026): no `CO-22-*.md`/`CO-23-*.md`/`CO-24-*.md` exists anywhere under `docs/CO/` (§5 reflects this). Full text is restored above (§2.3) from the original authoring source, cross-checked against the actual documents — this ledger entry is now the sole source of record for all three rulings, per §0's own "every CO lives in this one file" principle.

Restoring CO-22 surfaces a real problem this consolidation's own audit could not have caught, precisely because CO-22 was invisible to it: **CO-22 re-versions `32_responsive_strategy.md` v2.1→v2.2** (new §14, Single-Pane Content Width), authored the same day as CO-26 (Web Touch & Kiosk Compliance), which **also** re-versions v2.1→v2.2 (per this ledger's own §2.3 entry above) — for entirely different content. Neither author could have known about the other's v2.2; CO-22's was never in the repo for CO-26's session to find. **This ledger cannot resolve which v2.2 is authoritative in the real repo, or whether they've since been manually merged** — that requires reading the actual current `32_responsive_strategy.md` file on disk, which this filing does not have access to. **Flagged for the next session with repo access:** fetch the real current file, determine whether it carries CO-22's §14, CO-26's touch/kiosk rulings, both (already merged), or neither; reconcile using the same decisive-renumbering method §4.1/§4.8 already modeled for the CO-26/CO-27 collisions. **Likely outcome, not a decision made here:** both rulings survive as sequential versions rather than either being dropped — e.g. if CO-22's Single-Pane content keeps v2.2, CO-26's Touch/Kiosk rulings would need to land as v2.3, cascading CO-23's existing v2.3 (Toolbar) and CO-28's existing v2.4 (Tab Label) each up by one. **Do not assume either side wins, and do not renumber anything from this entry alone — check the real file first.**

**Addendum, folded in from this ledger's own earlier, narrower attempt at the same correction (not kept as a separate finding):** before the full-text restoration above was available, a session located CO-22 only via a citation inside `32_responsive_strategy_v2_4.md` §II.14 and reconstructed a partial picture from it — correctly flagging the original "NOT FOUND" verdict as wrong, but only able to reconstruct R11, not the fuller ruling now known above. That session's real-world finding still stands and is worth preserving: **CO-22's own text named an explicit, already-due retrofit that had never actually been implemented — discovered only when a live-app report traced back to this exact, already-ratified-but-unbuilt fix** (now folded into CO-22's own §2.3 entry above as its "Real-world status" bullet). **Recommend auditing whether any other CO named in a citing document's "Governed by"/binding table, but without its own file, has a similar built-vs-ratified gap** — CO-22 is proof this failure mode produces real shipped defects, not just documentation untidiness.

**4.11 — A THIRD numbering collision, same day, same number this ledger had just freed up.** `docs/CO/CO-28/CO-28-pre-auth-single-pane.md` ("Single-Pane Content Width, Pre-Auth Screens") independently self-identifies as CO-28 — the exact number §4.8 had just assigned (that same day) to the window-class tab-label ruling. Corroborating evidence this is a genuine, live collision and not a stale artifact: the pre-auth document's own text explicitly depends on CO-22's `SinglePaneContainer`/`single-pane-max-width` "already exist[ing] from the CO-22 Settings retrofit" — a retrofit that, per §4.10, was not actually *built* until this same session, meaning the pre-auth document was necessarily authored *after* both the CO-22 build and the §4.8 CO-28 assignment, yet still collided with it. **Decisive call, same pattern as §4.1/§4.8:** the tab-label ruling keeps CO-28 (already implemented in code, already this ledger's entry, multiple commits' worth of audit trail). The pre-auth single-pane ruling is renumbered **CO-29** (§2.3) — content unchanged, appended per this ledger's own rule. Its on-disk filename (`CO-28-pre-auth-single-pane.md`, plus its sibling `spec.md`/`expo-b2b-mobile.md`/`co-28-preauth-retrofit-prompt.md` in the same `docs/CO/CO-28/` folder) is left unrenamed, same precedent as §4.1/§4.8. **Pattern now confirmed three times in one ledger's lifetime — the underlying cause is structural, not incidental:** every collision so far has involved a number CO-101 itself either resolved or declared available *the same day* another document independently claimed it, with neither side aware of the other. Until every change order genuinely lives in this one file (this ledger's own stated goal), a next-available-number check against the Master Index alone is not sufficient — also grep the raw `docs/CO/` tree for the literal string before assigning any number, including "obviously free" ones.

**4.12 — CO-30 was briefly filed as a standalone file, violating this ledger's own §0 rule; caught and corrected same day.** A session ratifying the Stylist View-Scope finding (see §2.3, CO-30) read §1's Master Index, saw CO-1–CO-29 accounted for, and — without re-reading §0's operating rule first — created `docs/CO/CO-30-stylist-view-scope.md` as a standalone file, exactly the one-file-per-CO convention §0 explicitly retired "for CO-30 onward." Caught when the user flagged "CO-30 is already used" (accurately: the ledger's own §0/§12 header text already names CO-30 as the cutover number, making the standalone file doubly wrong — a fresh convention violation, not a content collision with prior work). **Corrected:** the standalone file deleted; its full content folded into this section (§2.3) per the rule that should have been followed the first time; downstream citations in `mobile-calendar/spec.md` and `quantum-schedule/spec.md` repointed from the filename to this ledger entry. **Confirms §4.11's closing recommendation was under-scoped:** that finding said "grep before assigning a number" to catch *content* collisions from parallel sessions; this incident shows the ledger's own *process* rule (§0) also has to be re-read at point of use, not just remembered from a prior pass — the Master Index alone doesn't carry the "no more files" instruction, only §0 does. **Standing rule for every future CO, restated because it was just missed once despite being written down:** before minting anything under `docs/CO/`, re-read §0 in full, not just the Master Index — for CO-30 onward the only correct action is a new dated entry under §2, appended directly into this file.

**Forward note (see §4.13): the ruling this finding is about was renumbered AGAIN, same day, from CO-30 to CO-31** — a second, unrelated problem (a cross-repo collision against `github.com/imakapp/Co-101`'s own independently-ratified CO-30) surfaced immediately after this finding was filed. This §4.12 entry's history is accurate as written for what happened at the time; "CO-30" in this entry's own text refers to the number as it stood before that second renumbering. Current, correct number is **CO-31** — see §2.3.

**4.13 — A numbering collision confirmed to operate ACROSS repos, not just within one: CO-30 independently claimed by both this ledger's home repo and `timetribe-platform`'s monorepo copy, same day.** Two sessions, working in different repos, each independently ratified a real, substantive CO-30 the same day (13 Jul 2026), neither aware of the other: **"Service Delivery Mode & Route Tab Capability Gating"** (`Studio.service_modes[]`/`Seat.mobile_enabled` fields, amends CO-27's Route-tab grant to an AND-condition — ratified directly in this repo) and **"Stylist View-Scope: Own + Assisted Clamp"** (SENIOR_STYLIST/JUNIOR_STYLIST calendar-visibility clamp — ratified in the `timetribe-platform` monorepo's own CO-101.md copy, which had additionally, separately, briefly violated its own §0 by filing it as a standalone file before self-correcting — see §4.12 above for the full account). Same failure mode as §4.1/§4.8, now confirmed to also operate across repos, not just within one file.

**Decisive call, same precedent as §4.1/§4.8:** whichever ruling was ratified in **`github.com/imakapp/Co-101`** first keeps the number — per direct user confirmation, this repo (not the monorepo's local copy) is the actively-maintained source of record for Change Orders. **Service Delivery Mode keeps CO-30.** Stylist View-Scope is renumbered **CO-31** (§2.3) — content unchanged, applied identically in both repos' copies (this repo's commit `4c47390`; monorepo's commit `edc0164`).

**Deeper fork found and reconciled the same session, beyond just this one numbering collision:** comparing the two files in full surfaced they had independently evolved past just this collision — different §4.10 content (since unified above, this same session), and the monorepo's copy had two further findings (§4.11, a THIRD same-repo collision; §4.12, the standalone-file mistake) that this repo's copy never had at all, since this repo's own history took cleaner paths to the same end states. **Both files are now reconciled to read identically as of this filing** — §4.1–§4.13 and §2.3's CO-22/23/24 full text are shared verbatim between `github.com/imakapp/Co-101` and `timetribe-platform`'s `docs/CO/CO-101.md`. Going forward, **always pull `github.com/imakapp/Co-101` fresh before ratifying any new CO or trusting this file's state** — it is the source of record; the monorepo copy is a manually-synced mirror, not the other way around.

---

## 5. Repo Hierarchy

```
F:\devops\timetribe-platform\
└── docs\
    └── change-orders\
        ├── CO-101.md                              ← THIS FILE — master ledger, active from 12 Jul 2026
        ├── co-16-vertical-seat-titling.md          (historical, unmodified, indexed §2.3)
        ├── CO-17-admission-tier-separation.md      (historical, unmodified, indexed §2.3)
        ├── CO-19-services-layout.md                (historical, unmodified, indexed §2.3)
        ├── CO-20-native-window-class.md            (historical — STALE local snapshot, see §4.5)
        ├── CO-21-pos-peripheral-hardware.md        (historical, unmodified, indexed §2.3)
        ├── CO-25-expanded-tier-parity.md           (historical, unmodified, indexed §2.3)
        ├── CO-26-web-touch-kiosk-compliance.md     (historical, unmodified — retains CO-26)
        ├── CO-26-role-nav-matrix.md                (historical, unmodified — content indexed as CO-27, see §4.1)
        ├── CO-27-window-class-tab-label.md         (historical, unmodified — content indexed as CO-28, see §4.8)
        └── CO-28-pre-auth-single-pane.md            (historical, unmodified — content indexed as CO-29, see §4.11)

Note: CO-1–CO-15 have no individual repo files — they exist only as table rows inside
      lexicon-validation-v1.md and spec-validation-report-v1.md (project knowledge, no /docs mount).
      CO-18, CO-22, CO-23, CO-24 have no dedicated repo file at all (see §4.2, §4.10) —
      CO-22's ruling was known via 32_responsive_strategy_v2_4.md before being fully
      restored directly into this ledger; confirmed by direct filesystem check (13 Jul
      2026) that no CO-22-*.md/CO-23-*.md/CO-24-*.md exists anywhere under docs/CO/.
      CO-29 has no repo file by design — per §0's "no new file" rule, it was authored
      directly as a CO-101 entry, never a standalone document.

Note (§4.9): the tree above shows the REAL path, docs/CO/ — every "Repo mount" field
      in this file (including its own header) says docs/change-orders/, which does not
      exist. Not corrected en masse here; flagged for a future pass.

From CO-30 onward: no new files land in docs/CO/ (or docs/change-orders/, per §4.9).
      New entries append directly into CO-101.md §2. Before assigning ANY number, including
      one that looks "obviously next," grep docs/CO/ for it first (see §4.11) — this ledger
      has been wrong about a number being free or absent three separate times in one day.
```

---

## 6. Sign-off

**Ratified:** Chief Architect, TimeTribe — 12 Jul 2026. **Amended six times since:**
- §4.8/§4.9 (12 Jul) — a second CO-27 collision surfaced during code implementation.
- §4.10 (12–13 Jul) — CO-22/23/24 restored to full text from the original authoring source (superseding this ledger's own earlier, narrower citation-only correction, folded in as this same finding's addendum); surfaces a still-unresolved `32_responsive_strategy` v2.2 version collision against CO-26.
- §4.11 (12 Jul) — a THIRD numbering collision: a pre-auth single-pane-width ruling independently also claimed CO-28; renumbered CO-29.
- §4.12 (13 Jul) — CO-30 (Stylist View-Scope, ratified in the `timetribe-platform` monorepo's own copy) briefly mis-filed as a standalone file there, in violation of this ledger's own §0 rule, self-corrected same day.
- §4.13 (13 Jul) — that same ruling turned out to ALSO collide cross-repo with this repo's own independently-ratified CO-30 ("Service Delivery Mode & Route Tab Capability Gating"); renumbered CO-31; **this file and the monorepo copy fully reconciled to read identically as of this filing** — both §2.3's CO-22/23/24 full text and §4's governance findings now match verbatim between the two.

**Consolidates:** CO-1 through CO-34 (34 entries; 4 renumbered [CO-27, CO-28, CO-29, CO-31]; 3 restored from citation-only to full text [CO-22, CO-23, CO-24]; 1 still genuinely citation-only [CO-18]; 0 confirmed absent). **CO-32/CO-33 (13 Jul 2026)** — first entries in this ledger authored directly by a Claude Chief-Architect session per explicit user directive ("take decision and necessary action, prior any action validate"); numbering verified free by grepping this file fresh-pulled from `main` immediately before filing, per §0's own rule. **CO-34 (13 Jul 2026, same day)** — closes CO-32 R3 (Utilisation Guard tier-eligibility, previously OPEN) and amends three CO-33 rulings (R1 Console Telemetry Pro, R2 Surge Shield status, R4 AI Command wording) on evidence gathered in a follow-on validation session; also ratifies Yield Shield's first-ever gate definition and a new Loyalty Ladder tier-included capability. Repo cloned fresh via `git clone` (not just a raw-file fetch) immediately before this entry was appended, confirming HEAD was still CO-33 (commit `66dad7f`) with no intervening writes.
**Open items requiring Chief Architect confirmation:** §4.6 (CO-5 completion), §4.7 (live Project Instructions phasing language), §4.9 (docs/CO/ vs docs/change-orders/ path convention), §4.10's `32_responsive_strategy` v2.2 collision (needs real-repo access to resolve) and its closing recommendation (audit other citing-only COs for the same built-vs-ratified gap CO-22 had), §4.11's closing recommendation (the collision pattern is structural, worth a process fix beyond "grep first"), CO-31's own open items (the M-02 v2 Phase 1 sequencing choice — now effectively resolved in favor of building directly against the corrected backend, per its own §2.3 entry — and the "assisted" data-model shape, R2, still fully open), **CO-34 R6 (Surge Shield — two conflicting specs need reconciliation, not authoring from scratch)**, **CO-34's own Follow-ups (`Pricing_Data_v2.0` full refresh — flagged, not actioned; only partially read at time of filing)**.
**Resolved by this filing's own amendments:** §4.1 (CO-27 renumbering), §4.8 (CO-28 renumbering), §4.11 (CO-29 renumbering), §4.13 (CO-31 renumbering, cross-repo reconciliation) — all decided, not open. §4.10's CO-22/23/24 restoration is decided; its `32_responsive_strategy` v2.2 collision sub-finding remains explicitly open, no repo access to verify against.
**Supersedes (process only):** the one-file-per-CO convention, for CO-30 forward. No prior ruling is reversed by this filing.
**This repo is the actively-maintained source of record for Change Orders** — per direct user instruction. Any other copy (e.g. `timetribe-platform`'s local `docs/CO/CO-101.md`) is a manually-synced mirror; pull this repo fresh before ratifying any new CO or trusting a mirror's state as current.

*End of CO-101.*
